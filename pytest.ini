[pytest]
# only look in the test/ directory
testpaths = test
# only pick up files starting with test_
python_files = test_*.py
# include project root on sys.path so imports like `from api_server import app` work
pythonpath = .
