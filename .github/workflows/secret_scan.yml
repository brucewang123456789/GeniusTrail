name: Secret Leak Scan
on:
  push:
    branches: [ main, deep-stress-test ]
  pull_request:

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v1
        with:
          args: "--path=. --verbose"
